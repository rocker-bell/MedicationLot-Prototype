/* Add these styles to your User_interface.css */
.login_register_container {
  position: relative;
  width: 200%; /* Double width to accommodate both forms */
  height: 100%;
  display: flex;
  transition: transform 0.6s ease-in-out;
  transform: translateX(0); /* Start with login form visible */
}

.user_interfaceLogin, .user_interfaceRegister {
  width: 50%; /* Each form takes half the container */
  flex-shrink: 0; /* Prevent flex items from shrinking */
  padding: 2rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 100%; /* Ensure full height */
  overflow-y: auto; /* Allow scrolling if content overflows */
}

/* Ensure the container only shows one form at a time */
.ContainerLogin_register {
  position: relative;
  width: 100%;
  max-width: 800px;
  height: 500px;
  background-color: white;
  border-radius: 15px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  overflow: hidden; /* Hide the second form when not active */
}
